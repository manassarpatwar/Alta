<h2>Complaints</h2>
<table>
    <tr>
        <th>ID</th>
        <th>Journey ID</th>
        <th>User ID</th>
        <th>Date Time</th>
        <th>Complaint</th>
        <th>Edit Record</th>
        <th>Delete Record</th>
    </tr>
    <% unless @complaintsInfo.nil? %>
        <% @complaintsInfo.each do |complaint| %>
        <tr>
            <td><%=h complaint[0] %></td>
            <td><%=h complaint[1] %></td>
            <td><%=h complaint[2] %></td>
            <td><%=h complaint[3] %></td>
            <td><%=h complaint[4] %></td>
            <td><a href='editComplaint/<%=h complaint[0] %>'>Edit</a></td>
            <td>
              <form method = "post" action = "/deleteFromTable" style = "margin:0;"> 
                  <input type = "hidden" name = "table" value = "complaints" />
                  <input type = "hidden" name = "id" value = "<%=h complaint[0] %>" />
                  <input type="submit" value="Delete"  onclick="return confirm('Are you sure you want to delete this record?');" style = "background:none; border:none; color:#0088cc; outline:none"/>      
              </form>
            </td>
        </tr>
        <% end %>
    <% end %>
</table>

<p>To add a new complaint click <a href="/addComplaint">here</a>.</p>
